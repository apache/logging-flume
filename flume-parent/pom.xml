<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one or more
  ~ contributor license agreements.  See the NOTICE file distributed with
  ~ this work for additional information regarding copyright ownership.
  ~ The ASF licenses this file to you under the Apache License, Version 2.0
  ~ (the "License"); you may not use this file except in compliance with
  ~ the License.  You may obtain a copy of the License at
  ~
  ~      http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" child.project.url.inherit.append.path="false" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.logging</groupId>
    <artifactId>logging-parent</artifactId>
    <version>12.1.1</version>
    <relativePath />
  </parent>

  <groupId>org.apache.flume</groupId>
  <artifactId>flume-parent</artifactId>
  <version>${revision}</version>
  <packaging>pom</packaging>

  <name>Apache Flume Parent</name>
  <description>Parent POM of all Apache Flume modules.
  It imports the Flume BOMs and provides the common test dependencies and build configuration, also for the satellite repositories.</description>
  <url>https://logging.apache.org/flume/2.x/index.html</url>

  <inceptionYear>2009</inceptionYear>

  <!-- Override the Log4j-specific mailing lists inherited from `logging-parent`. -->
  <mailingLists>
    <mailingList>
      <name>flume-users</name>
      <subscribe>flume-users-subscribe@logging.apache.org</subscribe>
      <unsubscribe>flume-users-unsubscribe@logging.apache.org</unsubscribe>
      <post>flume-users@logging.apache.org</post>
      <archive>https://lists.apache.org/list.html?flume-users@logging.apache.org</archive>
    </mailingList>
    <mailingList>
      <name>dev</name>
      <subscribe>dev-subscribe@logging.apache.org</subscribe>
      <unsubscribe>dev-unsubscribe@logging.apache.org</unsubscribe>
      <post>dev@logging.apache.org</post>
      <archive>https://lists.apache.org/list.html?dev@logging.apache.org</archive>
    </mailingList>
  </mailingLists>

  <scm child.scm.connection.inherit.append.path="false" child.scm.developerConnection.inherit.append.path="false" child.scm.url.inherit.append.path="false">
    <connection>scm:git:https://gitbox.apache.org/repos/asf/logging-flume.git</connection>
    <developerConnection>scm:git:https://gitbox.apache.org/repos/asf/logging-flume.git</developerConnection>
    <tag>rel/${revision}</tag>
    <url>https://gitbox.apache.org/repos/asf/logging-flume.git</url>
  </scm>

  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/apache/logging-flume/issues</url>
  </issueManagement>

  <ciManagement>
    <system>GitHub Actions</system>
    <url>https://github.com/apache/logging-flume/actions</url>
  </ciManagement>

  <properties>

    <revision>2.0.0-SNAPSHOT</revision>

    <!-- Version of the artifacts released from the main `logging-flume` repository -->
    <flume-project.version>${revision}</flume-project.version>
    <!-- The website is generated by Antora from `flume-project`: skip the per-module `maven-site-plugin` sites. -->
    <maven.site.skip>true</maven.site.skip>
    <!-- Dummy value; overwritten by CI at release time. Do not edit manually. -->
    <project.build.outputTimestamp>2026-01-01T00:00:00Z</project.build.outputTimestamp>

    <!-- Append build metadata to the manifest generated by BND in `logging-parent`.
         `Bundle-SCM` is specified by OSGi Core R8, section 3.2.1; `logging-parent` strips the header
         because inheritance assembly corrupts `project.scm.url`. Flume sets
         `child.scm.*.inherit.append.path="false"`, so its value is correct: keep the header.
         Neither the JAR File Specification nor OSGi define a header for the build timestamp or the
         Package URL, so `Implementation-Timestamp` and `Purl` are Flume-specific. -->
    <bnd-extra-config>-removeheaders: Bundle-DocURL,Bundle-Developers

      Implementation-Timestamp: ${project.build.outputTimestamp}
      Purl: pkg:maven/${project.groupId}/${project.artifactId}@${project.version}</bnd-extra-config>
    <!-- Set default encoding to UTF-8 to remove maven complaints -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Flume generates Java 17 bytecode. Disable all compiler switches except `release` -->
    <maven.compiler.source />
    <maven.compiler.target />
    <maven.compiler.release>17</maven.compiler.release>

    <!-- defaults for flaky test and focused test exclusions -->
    <test.exclude.pattern>$</test.exclude.pattern>
    <!-- junk pattern -->
    <test.include.pattern>**/Test*.java</test.include.pattern>
    <redirectTestOutput>true</redirectTestOutput>

    <awaitility.version>4.3.0</awaitility.version>
    <curator.version>5.9.0</curator.version>
    <fest-reflect.version>1.4.1</fest-reflect.version>
    <hadoop.version>3.5.0</hadoop.version>
    <junit.version>4.13.2</junit.version>
    <mockito.version>5.23.0</mockito.version>
    <mvn-checksum-plugin.version>1.11</mvn-checksum-plugin.version>
    <mvn-pmd-plugin.version>3.28.0</mvn-pmd-plugin.version>
    <external.protobuf.version>4.35.0</external.protobuf.version>
    <protobuf.plugin.version>0.6.1</protobuf.plugin.version>
    <os.maven.version>1.7.1</os.maven.version>
    <system-rules.version>1.19.0</system-rules.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.apache.flume</groupId>
        <artifactId>flume-third-party-dependencies</artifactId>
        <version>${flume-project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.awaitility</groupId>
        <artifactId>awaitility</artifactId>
        <version>${awaitility.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>${junit.version}</version>
      </dependency>

      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.easytesting</groupId>
        <artifactId>fest-reflect</artifactId>
        <version>${fest-reflect.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>com.github.stefanbirkner</groupId>
        <artifactId>system-rules</artifactId>
        <version>${system-rules.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-minikdc</artifactId>
        <version>${hadoop.version}</version>
        <scope>test</scope>
        <exclusions>
          <exclusion>
            <groupId>ch.qos.reload4j</groupId>
            <artifactId>reload4j</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-reload4j</artifactId>
          </exclusion>
          <exclusion>
            <!-- exclude this from hadoop minikdc as the minikdc depends on
            the apacheds-jdbm1 bundle, which is not available in maven central-->
            <groupId>org.apache.directory.jdbm</groupId>
            <artifactId>apacheds-jdbm1</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <!-- add this to satisfy the dependency requirement of apacheds-jdbm1 in minikdc-->
        <groupId>org.apache.directory.jdbm</groupId>
        <artifactId>apacheds-jdbm1</artifactId>
        <version>2.0.0-M3</version>
        <scope>test</scope>
      </dependency>

      <!-- internal module dependencies -->
      <dependency>
        <groupId>org.apache.flume</groupId>
        <artifactId>flume-dependencies</artifactId>
        <version>${flume-project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.flume</groupId>
        <artifactId>build-support</artifactId>
        <version>${flume-project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.apache.flume</groupId>
        <artifactId>flume-ng-sdk-test</artifactId>
        <version>${flume-project.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.curator</groupId>
        <artifactId>curator-test</artifactId>
        <version>${curator.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-minicluster</artifactId>
        <version>${hadoop.version}</version>
        <scope>test</scope>
        <exclusions>
          <exclusion>
            <groupId>tomcat</groupId>
            <artifactId>jasper-compiler</artifactId>
          </exclusion>
          <exclusion>
            <groupId>tomcat</groupId>
            <artifactId>jasper-runtime</artifactId>
          </exclusion>
          <exclusion>
            <groupId>ch.qos.reload4j</groupId>
            <artifactId>reload4j</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-reload4j</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-mapreduce-client-core</artifactId>
        <version>${hadoop.version}</version>
        <scope>test</scope>
        <exclusions>
          <exclusion>
            <groupId>ch.qos.reload4j</groupId>
            <artifactId>reload4j</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-reload4j</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>org.mock-server</groupId>
        <artifactId>mockserver-netty</artifactId>
        <version>7.4.0</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mock-server</groupId>
        <artifactId>mockserver-client-java</artifactId>
        <version>3.10.8</version>
        <scope>test</scope>
      </dependency>

      <!-- Transitive dependencies pinned to satisfy `requireUpperBoundDeps`,
           so that resolution does not depend on declaration order.

           If the check fails here, try:
           1. Removing the pin first,
           2. Otherwise, bump to the highest version required by our direct dependencies.

           Dependabot must *not* upgrade these dependencies.
        -->
      <dependency>
        <groupId>com.google.errorprone</groupId>
        <artifactId>error_prone_annotations</artifactId>
        <version>2.50.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>

    <pluginManagement>
      <plugins>

        <plugin>
          <groupId>org.xolstice.maven.plugins</groupId>
          <artifactId>protobuf-maven-plugin</artifactId>
          <version>${protobuf.plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <reuseForks>false</reuseForks>
            <forkCount>1</forkCount>
            <forkedProcessTimeoutInSeconds>900</forkedProcessTimeoutInSeconds>
            <redirectTestOutputToFile>${redirectTestOutput}</redirectTestOutputToFile>
            <argLine>-Djava.net.preferIPv4Stack=true</argLine>
            <includes>
              <include>${test.include.pattern}</include>
            </includes>
            <excludes>
              <exclude>**/*$*</exclude>
              <exclude>${test.exclude.pattern}</exclude>
            </excludes>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-pmd-plugin</artifactId>
          <version>${mvn-pmd-plugin.version}</version>
          <configuration>
            <targetJdk>${maven.compiler.release}</targetJdk>
            <excludeRoots>
              <excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
            </excludeRoots>
          </configuration>
          <executions>
            <execution>
              <id>verify.pmd</id>
              <goals>
                <goal>check</goal>
              </goals>
              <phase>verify</phase>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>net.nicoulaj.maven.plugins</groupId>
          <artifactId>checksum-maven-plugin</artifactId>
          <version>${mvn-checksum-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>

      <!-- Append the Flume-specific exclusions to the Spotless configuration of `logging-parent`. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <configuration>
          <java>
            <excludes>
              <!-- Third-party vendored sources: ASF policy forbids adding the ASF header to them. -->
              <exclude>**/source/shaded/guava/**</exclude>
            </excludes>
          </java>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
      </plugin>

      <!-- Ban logging implementations (not logging APIs) that dependencies
           incorrectly add as compile dependencies -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>ban-logging-dependencies</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <bannedDependencies>
                  <excludes>
                    <!-- JCL replacement -->
                    <exclude>org.springframework:spring-jcl</exclude>
                    <!-- Log4j 1.x -->
                    <exclude>log4j:log4j</exclude>
                    <!-- Groups with logging dependencies -->
                    <exclude>ch.qos.logback:*</exclude>
                    <exclude>ch.qos.reload4j:*</exclude>
                    <exclude>org.apache.logging.log4j:*</exclude>
                    <exclude>org.jboss.logging:*</exclude>
                    <exclude>org.slf4j:*</exclude>
                    <exclude>org.ops4j.pax.logging:*</exclude>
                  </excludes>
                  <includes>
                    <!-- As compile dependencies (including transitive): only real logging APIs -->
                    <include>commons-logging:commons-logging</include>
                    <include>org.apache.logging.log4j:log4j-api</include>
                    <include>org.jboss.logging:jboss-logging</include>
                    <include>org.slf4j:slf4j-api</include>
                    <!-- As a test dependency -->
                    <include>org.apache.logging.log4j:log4j-core:*:jar:test</include>
                    <include>org.apache.logging.log4j:log4j-jcl:*:jar:test</include>
                    <include>org.apache.logging.log4j:log4j-slf4j2-impl:*:jar:test</include>
                  </includes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>

    <extensions>
      <extension>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>${os.maven.version}</version>
      </extension>
    </extensions>

  </build>

  <profiles>

    <!-- Read additional RAT exclusions from a `.rat-excludes` file at the root of the repository, if present.
         Guarded by a profile, since the plugin fails if `excludesFile` points to a missing file. -->
    <profile>
      <id>rat-excludes-file</id>
      <activation>
        <file>
          <exists>${maven.multiModuleProjectDirectory}/.rat-excludes</exists>
        </file>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.rat</groupId>
            <artifactId>apache-rat-plugin</artifactId>
            <configuration>
              <excludesFile>${maven.multiModuleProjectDirectory}/.rat-excludes</excludesFile>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>skipChecks</id>
      <properties>
        <spotbugs.skip>true</spotbugs.skip>
        <pmd.skip>true</pmd.skip>
        <rat.skip>true</rat.skip>
        <spotless.check.skip>true</spotless.check.skip>
      </properties>
    </profile>
  </profiles>
</project>
